# Application dependencies

require:
    - play
    - play -> less 0.9.1
    - localModule -> twitterBootstrap 2.0.3 

repositories:
    - localModule:
        type: local
        artifact: "${application.path}/../../dist/[module]-[revision].zip"
        contains:
            - localModule -> *
